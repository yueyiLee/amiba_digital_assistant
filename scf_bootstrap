#!/bin/sh
# CloudBase HTTP Web function bootstrap (must listen on 9000)
# Use sh not bash: SCF Node.js runtime only has sh, no bash
export PORT=9000
if [ -d nm ] && [ ! -d node_modules ]; then
  mv nm node_modules
fi
node server.js
